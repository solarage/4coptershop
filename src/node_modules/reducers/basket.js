import {
    ADD_QUADCOPTER_TO_BASKET
} from 'actionTypes';

const initialState = [];

export default (state = initialState, action) => {
    switch(action.type) {
        case ADD_QUADCOPTER_TO_BASKET:
            return [
                ...state,
                action.payload
            ]
            
        default:
            return state
    }
}